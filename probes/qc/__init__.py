"""The QC cell's own logic: where its stations are, and how a round runs.

A package rather than another script, because the tests import it and because the
eventual move into `qc:cell` verbs should be a rename, not a rewrite.
"""
